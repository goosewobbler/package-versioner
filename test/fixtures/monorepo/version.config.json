{
  "preset": "conventional-commits",
  "packages": [
    "packages/**/*"
  ],
  "skip": [
    "@scope/package-a"
  ],
  "versionPrefix": "v",
  "tagTemplate": "${prefix}${version}",
  "packageTagTemplate": "${packageName}@${prefix}${version}"
}